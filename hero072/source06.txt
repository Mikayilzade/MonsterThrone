.lineTo(x-8,y-10);ctx.lineTo(x+6,y-15);ctx.lineTo(x+16,y+8);ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(230,240,235,.18)';ctx.stroke();}
function drawGrass(x,y,swamp){ctx.strokeStyle=swamp?'#7da06d':'#8eb968';ctx.lineWidth=1.5;for(let i=-1;i<=1;i++){ctx.beginPath();ctx.moveTo(x+i*3,y+8);ctx.lineTo(x+i*5,y-6);ctx.stroke();}}
function drawCamp(c){const x=world.camp.x-c.x,y=world.camp.y-c.y;ctx.fillStyle='rgba(112,213,177,.045)';ctx.beginPath();ctx.arc(x,y,CAMP_SAFE_RADIUS,0,TAU);ctx.fill();ctx.strokeStyle='rgba(112,213,177,.72)';ctx.lineWidth=3;ctx.setLineDash([12,8]);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='rgba(243,171,72,.22)';ctx.beginPath();ctx.arc(x,y,65,0,TAU);ctx.fill();ctx.fillStyle='#bc7041';ctx.beginPath();ctx.moveTo(x-22,y+18);ctx.lineTo(x,y-28);ctx.lineTo(x+24,y+18);ctx.closePath();ctx.fill();ctx.fillStyle='#ffd17b';ctx.beginPath();ctx.arc(x+31,y+11,6,0,TAU);ctx.fill();ctx.fillStyle='#e9d2a0';ctx.font='12px system-ui';ctx.fillText('Лагерь',x-22,y+42);}
function drawObjects(c){for(const o of world.objects){const x=o.x-c.x,y=o.y-c.y;if(x<-40||y<-40||x>W+40||y>H+40)continue;if(o.kind==='plant'){ctx.font='18px "Segoe UI Emoji"';ctx.textAlign='center';ctx.fillText(ITEMS[o.id].icon,x,y+6);}else if(o.kind==='resource'){ctx.font='16px "Segoe UI Emoji"';ctx.textAlign='center';ctx.fillText(ITEMS[o.id].icon,x,y+6);}else if(o.kind==='trap'){ctx.font='20px "Segoe UI Emoji"';ctx.fillText('🪤',x,y+6);}}
}
function drawCarcasses(c){
  const pileLayout=MOBILE.corpsePileLayout(world.carcasses);
  for(const o of world.carcasses){
    ensureCarcass(o);
    const pile=pileLayout.get(o),x=o.x+pile.x-c.x,y=o.y+pile.y-c.y;if(x<-50||y<-50||x>W+50||y>H+50)continue;
    const fr=carcassFreshness(o);
    ctx.fillStyle=o.age<300?'rgba(105,29,27,.78)':'rgba(88,67,48,.8)';
    ctx.beginPath();ctx.ellipse(x,y,18,10,0,0,TAU);ctx.fill();
    if(selectedCarcass===o){ctx.strokeStyle='#f0d278';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(x,y,25,17,0,0,TAU);ctx.stroke();}
    ctx.fillStyle=fr.color;ctx.font='10px system-ui';ctx.textAlign='center';
    if(pile.label)ctx.fillText(pile.count>1?`туши ×${pile.count} · частей ${pile.parts}`:`${fr.label} · ${o.parts.length}`,x,y-15);
  }
}
function drawEnemies(c){for(const e of world.enemies){if(e.dead)continue;const d=ENEMY_TYPES[e.type],x=e.x-c.x,y=e.y-c.y;if(x<-60||y<-60||x>W+60||y>H+60)continue;ctx.save();ctx.translate(x,y);ctx.fillStyle='rgba(0,0,0,.3)';ctx.beginPath();ctx.ellipse(2,d.radius*.65,d.radius*1.1,d.radius*.38,0,0,TAU);ctx.fill();ctx.fillStyle=d.color;ctx.beginPath();ctx.arc(0,0,d.radius,0,TAU);ctx.fill();ctx.fillStyle='rgba(255,255,255,.16)';ctx.beginPath();ctx.arc(-d.radius*.25,-d.radius*.3,d.radius*.3,0,TAU);ctx.fill();if(e.type==='boar'){ctx.strokeStyle='#ded1a2';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(-d.radius,-2);ctx.lineTo(-d.radius-8,6);ctx.moveTo(d.radius,-2);ctx.lineTo(d.radius+8,6);ctx.stroke();}if(e.type==='wolf'){ctx.fillStyle='#b7c2d2';ctx.beginPath();ctx.moveTo(-9,-15);ctx.lineTo(-4,-29);ctx.lineTo(2,-14);ctx.moveTo(9,-15);ctx.lineTo(4,-29);ctx.lineTo(-2,-14);ctx.fill();}ctx.restore();if(target===e){ctx.strokeStyle='#ffe27a';ctx.lineWidth=3;ctx.setLineDash([7,4]);ctx.beginPath();ctx.arc(x,y,d.radius+10,0,TAU);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#ffe27a';ctx.beginPath();ctx.moveTo(x,y-d.radius-18);ctx.lineTo(x-7,y-d.radius-29);ctx.lineTo(x+7,y-d.radius-29);ctx.closePath();ctx.fill();ctx.fillStyle='rgba(14,26,28,.8)';ctx.fillRect(x-25,y-d.radius-18,50,5);ctx.fillStyle='#e46c65';ctx.fillRect(x-25,y-d.radius-18,50*(e.hp/e.maxHp),5);}}
}
function drawHero(c){const x=hero.x-c.x,y=hero.y-c.y;ctx.save();ctx.translate(x,y);const a=target&&!target.dead?Math.atan2(target.y-hero.y,target.x-hero.x):lastInputWasTouch?Math.atan2(lastAimDirection.y,lastAimDirection.x):Math.atan2(mouse.worldY-hero.y,mouse.worldX-hero.x);ctx.fillStyle='rgba(223,189,102,.1)';ctx.beginPath();ctx.moveTo(0,0);ctx.arc(0,0,72,a-.6,a+.6);ctx.closePath();ctx.fill();ctx.fillStyle=(hero.invulnerableUntil||0)>world.time?'rgba(245,215,112,.32)':'rgba(60,225,200,.16)';ctx.beginPath();ctx.arc(0,0,(hero.invulnerableUntil||0)>world.time?27:22,0,TAU);ctx.fill();ctx.fillStyle='#d9eee8';ctx.beginPath();ctx.arc(0,-4,9,0,TAU);ctx.fill();ctx.fillStyle='#3c756a';ctx.fillRect(-7,4,14,18);ctx.strokeStyle='#c7b37b';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(Math.cos(a)*5,Math.sin(a)*5);ctx.lineTo(Math.cos(a)*34,Math.sin(a)*34);ctx.stroke();ctx.restore();}
function drawMinimap(){
  const layout=MOBILE.minimapLayout(W,H,innerWidth>innerHeight),r=layout.r,x=layout.x,y=layout.y,s=r/world.radius;
  ctx.save();ctx.globalAlpha=.94;ctx.fillStyle='rgba(2,10,14,.94)';ctx.beginPath();ctx.arc(x,y,r,0,TAU);ctx.fill();ctx.save();ctx.beginPath();ctx.arc(x,y,r,0,TAU);ctx.clip();
  const cell=Math.max(1,sectorConfig.sectorSize*s);
  for(const id of sectorManager.visited){const [sx,sy]=id.split(',').map(Number),wx=sx*sectorConfig.sectorSize,wy=sy*sectorConfig.sectorSize,b=terrainAt(wx+sectorConfig.sectorSize/2,wy+sectorConfig.sectorSize/2);ctx.fillStyle=terrainColor[b]||terrainColor.water;ctx.fillRect(x+(wx-world.cx)*s,y+(wy-world.cy)*s,cell+1,cell+1);}
  ctx.restore();ctx.strokeStyle='rgba(170,220,210,.4)';ctx.beginPath();ctx.arc(x,y,r,0,TAU);ctx.stroke();ctx.fillStyle='#78e2c0';ctx.beginPath();ctx.arc(x+(hero.x-world.cx)*s,y+(hero.y-world.cy)*s,4,0,TAU);ctx.fill();
  if(sectorManager.visited.has(`${Math.floor(world.camp.x/sectorConfig.sectorSize)},${Math.floor(world.camp.y/sectorConfig.sectorSize)}`)){ctx.fillStyle='#f2b968';ctx.beginPath();ctx.arc(x+(world.camp.x-world.cx)*s,y+(world.camp.y-world.cy)*s,3,0,TAU);ctx.fill();}ctx.restore();
}

function loop(now){const dt=Math.min(.05,(now-lastFrame)/1000);lastFrame=now;update(dt);draw();if(!$('hud').classList.contains('hidden'))requestAnimationFrame(loop);}

// Mobile controls
const joy=$('joystick'),stick=$('stick');let joyPointer=null;
function joyMove(e){const r=joy.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=e.clientX-cx,dy=e.clientY-cy,l=Math.hypot(dx,dy),m=Math.min(36,l),nx=l?dx/l:0,ny=l?dy/l:0;stick.style.transform=`translate(${nx*m}px,${ny*m}px)`;mobileVec={x:nx*Math.min(1,l/36),y:ny*Math.min(1,l/36)};if(l>5)lastAimDirection={x:nx,y:ny};lastInputWasTouch=true;lastMobileAction='joystick';e.preventDefault();e.stopPropagation();}
joy.addEventListener('pointerdown',e=>{joyPointer=e.pointerId;joy.setPointerCapture(e.pointerId);joyMove(e);});joy.addEventListener('pointermove',e=>{if(e.pointerId===joyPointer)joyMove(e);});joy.addEventListener('pointerup',e=>{if(e.pointerId===joyPointer){e.preventDefault();e.stopPropagation();joyPointer=null;mobileVec={x:0,y:0};stick.style.transform='';}});joy.addEventListener('pointercancel',e=>{if(e.pointerId===joyPointer){joyPointer=null;mobileVec={x:0,y:0};stick.style.transform='';}});
document.querySelectorAll('[data-mobile-action]').forEach(b=>b.onclick=()=>{lastInputWasTouch=true;lastMobileAction=b.dataset.mobileAction;({attack:()=>attack(true),dodge:()=>dodge(true),pickup:pickupNearby,use:useNearby})[b.dataset.mobileAction]();});
MOBILE.bindPointerButtons(document,b=>{lastInputWasTouch=true;lastMobileAction=b.dataset.mobileAction||b.id||b.dataset.panel||'button';});

// Debug surface used by local tests.
function debugStep(dt){const was=world.paused;world.paused=false;update(dt);world.paused=was;}
const debugApi={SECTORS,sectorManager,ITEMS,PLANT_TRUTH,RECIPES,HIDDEN_REACTIONS,EXPERIMENT_METHODS,hero,world,terrainAt,resetHeroNew,saveGame,loadGame,craftRecipe,experiment,addInv,removeInv,debugStep,draw,skillFactor,supportedCeiling,addStatPotential,recomputeDerived,growthSpeed,combatProfile,equipWeapon,hitFormulaText,butcheryFactor,RULES,mobileDiagnostics:()=>({lastMobileAction,lastAimDirection:{...lastAimDirection},selectedQuickSlot:hero.selected,invulnerabilityRemaining:Math.max(0,(hero.invulnerableUntil||0)-world.time),heroInSafeZone:MOBILE.inSafeZone(hero,world.camp,CAMP_SAFE_RADIUS)})};window.MonsterThroneV08=debugApi;window.MonsterThroneV07=debugApi;window.MonsterThroneV06=debugApi;

renderHotbar();
})();
